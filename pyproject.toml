[tool.pytest.ini_options]
DJANGO_SETTINGS_MODULE = "aigitgateway.settings.test"
python_files = ["test_*.py"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
